# @vertexa/prisma-config

Fork of [`@prisma/config`](https://github.com/prisma/prisma) — loader for `prisma.config.ts`.

Internal package used by `@vertexa/prisma` CLI to define and read Prisma
configuration files. Do not depend on it directly — it is bundled into the CLI
at build time.
